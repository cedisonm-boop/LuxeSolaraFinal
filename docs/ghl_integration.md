# GoHighLevel Integration

GoHighLevel synchronization is asynchronous: save submission, evaluate, persist result, return to user, queue delivery, retry failures. Mappings live in configuration and credentials live in environment variables. The adapter can find/create contacts, update custom fields, tag contacts, and record sanitized delivery responses.

Embed snippet:
```html
<luxe-solara-assessment assessment-key="fit-check" api-base-url="https://api.example.com"></luxe-solara-assessment>
<script type="module" src="https://cdn.example.com/luxe-solara-assessment.js"></script>
```
