# Security and Privacy

Controls include CORS allowlists, correlation IDs, public result tokens with high entropy, no PII in URLs, structured logs without full request bodies, admin password hashing, RBAC-ready models, queued integrations, idempotency keys, and immutable audit/evaluation records. CAPTCHA and payment verification are adapter interfaces.
