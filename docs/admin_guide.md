# Admin Guide

Administrators manage draft assessments, question options, rule sets, programs, result templates, submissions, manual reviews, overrides, simulations, and integration delivery replay through `/admin` and `/api/v1/admin`. Published versions are immutable; clone to draft for changes, validate, simulate, then publish.
